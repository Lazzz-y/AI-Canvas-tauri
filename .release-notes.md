## ✨ 版本摘要

AI Canvas v0.9.14 集成更多 ComfyUI 内置工作流（H3 PDD 系列与 Breeze 语音工作流），支持配置、图像与参考图三类素材按引用顺序自动匹配上传；模型协议编辑器新增「复制协议给 AI 修改」，便于快速迭代协议配置。同时优化素材路由、画布复制与 MCP 媒体节点同步，并在开发环境下记录原生 HTTP 错误响应体并脱敏，方便排查请求失败。

## 🧩 ComfyUI 内置工作流

- 内置 H3 PDD 系列工作流：图生视频（i2v）、带音频图生视频（i2v-audio）、自由参考图生视频（r2v），并修复空素材参考的问题。
- 内置 Breeze 语音工作流：声音克隆（tts2-voice-clone）与声音设计（tts2-voice-design）。

## 📤 素材自动匹配上传

- 支持配置、图像与参考图三类素材按引用顺序自动匹配上传，优化工作流工具的素材路由逻辑。

## 🛠️ 模型协议编辑器

- 新增「复制协议给 AI 修改」功能，一键将当前协议复制到对话中交由 AI 调整，便于快速迭代协议配置。

## 🐛 修复

- 修复拖拽复制时原节点与副本身份颠倒的问题。
- 同步媒体节点显示名称，并补充标题读回，MCP 媒体工具展示更准确。
- HTTP 客户端在开发环境下记录原生错误响应体（上限 8KB）并脱敏（隐藏 Bearer Token、密钥及 URL 中的敏感字段），便于排查请求失败。

## ⚠️ 升级与兼容性

- 升级后已有插件会在首次加载时补全源码摘要，无需重新安装。
- 私有注册表目录（`plugin-private`）被用户手动删除时，已有插件会停用并要求重新安装；这是预期的失败关闭行为，不会从浏览器存储静默恢复。
- RunningHub 与 AutoDL 工作流需要配置对应的服务地址与凭据。
- Blender 原生运行时需要本机已安装 Blender，且需授权项目目录。
- Windows 旧款 CPU（不支持 AVX/AVX2）请使用名称含 `Legacy` 的安装包；该版本不包含本地 ONNX 功能。
- 本地语音转文本依赖桌面端 ONNX 运行时；Intel Mac 和 Windows Legacy 安装包不包含该功能。
- 视频剪辑器依赖本地编解码能力，实际可用导出编码格式取决于本机编解码器。
- 浏览器模式无法使用桌面端凭据存储，API Key 仅在当前会话有效，重启后需要重新填写。
- 视频剪辑窗口、应用内 ComfyUI、MCP 控制桥、长按圆环和本地文件导出仅在桌面版可用。

## 📦 下载

- 🪟 Windows：使用 `.exe` 或 `.msi`。
- 🪟 Windows 旧款 CPU：使用名称含 `Legacy` 的 `.exe` 或 `.msi`；该版本不包含本地 ONNX 功能。
- 🍎 macOS：Apple Silicon 使用 `aarch64.dmg`，Intel 使用 `x64.dmg`。
- 🐧 Linux：使用 `.AppImage`、`.deb` 或 `.rpm`。
