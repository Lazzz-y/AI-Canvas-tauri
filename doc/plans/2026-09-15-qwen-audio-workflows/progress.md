# 交付与验证

- 已按用户最新请求仅内置 01 原声1比1克隆、02 文生语音抽卡、03 参考音频抽卡-支持方言；前四份没有加入。
- 新增六份 API/UI 资源，三项 ai-audio 注册；默认与显式台词支持 target_text，参考转写连线保持。
- 声音设计节点按编辑图 randomize 设置在每次音频提交时换种子；固定种子、连线、旁路和无有效编辑图时保留原值，不修改工作流库中的原图。
- 三份 UI 与来源逐字节一致；严格 UTF-8、API 连线与本机 Qwen INPUT_TYPES 字段校验通过。
- builtinWorkflows 与 comfyWorkflowAudioIO 定向检查：54 项通过。
- 默认 IO、保存回写、编辑器、audioSpeechSettings 回归检查：76 项通过。
- npm run typecheck、npm run test:typecheck、三个修改 TypeScript 文件的定向 ESLint、git diff --check 通过。
- 初次两项上传断言遇到现有上传缓存复用，测试改用独立音频数据后通过，未修改缓存实现。
- 一次 rg 的 PowerShell 通配路径错误已改用实际路径；无未解决工具错误。
- 保留用户 .gitignore 与 site/manual.pdf；未修改 README、运行环境或依赖，未打包、启动或真实生成。
- 第 03 项 SeedVCVoiceConversion 的实际安装与运行、三项生成听感均未验收。

## 第二阶段

- 用户批准九份产品文件范围；完成 Qwen 参数声明、分组面板、按工作流独立保存和提交注入，保留 AuK。
- 98 项定向测试通过，覆盖能力识别、默认值、字段映射、参考缺失、固定／抽卡、显式样本优先、服务端 SeedVC 范围和旧 AuK 回归。
- 前端类型、测试类型和八份 TypeScript/TSX 定向 ESLint 通过。
- 实际组件独立预览检查浅／深主题、参考添加移除、声音预设、固定／抽卡模式、转换开关、折叠与滚动；发现描述框高度不足后改用现有 ui-textarea。
- 复查确认自定义描述编辑、恢复原图默认值和清除当前工作流覆盖生效；03 两条合成链路增加可见分组标题。
- 最终 98 项测试与定向 ESLint 再次通过；git diff --check 和 19 份任务文件严格 UTF-8 检查通过。
- 独立预览重建曾漏配 automatic JSX，已按项目设置修正并重新验证；无产品运行时改动。
- 不代表完整桌面或真实模型验收；未启动 ComfyUI、下载模型或打包发布。
- 提交前核对暂存白名单共 19 份文件。暂存差异检查发现三份原始 UI 图自带末尾空行，为保持来源原样保留；排除该项后其余空白检查通过。此前工作区差异检查未覆盖未跟踪文件。
