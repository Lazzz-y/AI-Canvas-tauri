# 模型与生成

负责模型目录、厂商协议、图片/视频/音频生成及工作流适配。生成成功、取消、产物保存是需要分别处理的状态。

## 主要入口

| 入口 | 职责 |
|---|---|
| [generationRuntime.ts](../src/services/ai/generationRuntime.ts) | 对话媒体生成：解析模型、分派生成并保存产物 |
| [mediaProviderRegistry.ts](../src/services/ai/mediaProviderRegistry.ts) | 参考媒体与 Provider 能力分派 |
| [modelProtocol.ts](../src/services/ai/modelProtocol.ts) / [providers](../src/services/ai/providers/) | 声明式模型协议与厂商适配 |
| [imageRequestContracts.ts](../src/services/ai/imageRequestContracts.ts) | 内置 CCC / GRSAI 图片合同选择、参考图传输与厂商尺寸映射；复用标准图片和声明式协议执行器 |
| [referenceImageUpload.ts](../src/services/ai/referenceImageUpload.ts) / [uploadService.ts](../src/services/uploadService.ts) | 共享参考图压缩、透明像素检测、受限并发及按目标上传；保留本地原图 |
| [media.ts](../src/types/media.ts) | 媒体生成意图、结果与持久化状态合同 |
| [comfyWorkflowService.ts](../src/services/comfyWorkflowService.ts) / [comfyProgress.ts](../src/services/comfyProgress.ts) | ComfyUI 输入注入、可选分支清理、执行与实时节点进度 |
| [comfyMemory.ts](../src/services/comfyMemory.ts) | ComfyUI 显存读取、手动释放及本地空闲队列的可选自动释放 |
| [workflowApi](../src/services/workflowApi/) / [workflowExecutionService.ts](../src/services/workflowExecutionService.ts) | 工作流 API 合同与执行；与 RunningHub 共用工作流绑定和云产物保存 |

## 关键边界

- 对话生成走统一入口；节点生成沿用节点/生成服务，不能声称所有入口已统一迁移。
- 工作流与厂商模型 API 使用各自适配协议；鉴权、上传、轮询和解析不散落在节点组件中。
- 参考图按已声明协议传输：标准 `edits-multipart` 直接读取原图，不先上传第三方图床；高级自定义协议优先保留自身路径、字段和响应/轮询映射，multipart 的 `$file` 图片引用转换为受限 data URL 后编码为文件。`$file` 可接收数组并按原字段名提交多个文件；请求预览逐文件脱敏。APIMart 继续使用自身上传服务，未确认支持 multipart 的厂商不自动切换或失败后重提生成。
- 内置图片合同由 `resolveBuiltInImageRequestContract` 统一选择：CCC 目录中声明 `edits-multipart` 的图片模型使用 OpenAI Images；GRSAI 文档列出的 Nano Banana / GPT Image 使用原生 JSON。已确认合同执行时优先于旧 Provider、旧通用模型保存的参考图模式或 `executionProfile`；不迁移持久化配置。其他厂商、自定义连接、未收录模型与工作流保持原协议优先级，未知厂商不按模型名称猜协议。multipart 和 data URL 请求中的提示词参考图优先使用本地素材，不复用生成结果的远程 sourceUrl。自定义协议缺少参考图映射时仍在本地拦截。
- 参考图压缩复用 `prepareReferenceImageUpload`：标准 multipart、GRSAI/自定义 Base64、自定义文件表单、APIMart/通用图床、RunningHub 图片及工作流内联图片均接入。直接交给服务商读取的公网 URL 不额外下载转存；有效上传缓存继续复用，视频/音频保持原处理方式。
- 共享预处理先读取图片头：JPEG、静态 PNG、静态 WebP 的最长边达到 3840 px 时等比缩到 2048 px，再按透明像素输出 PNG 或 JPEG；不放大小图，超过 2400 万像素的超大源图仍保留原字节。未达到 4K 的图片继续沿用原规则：只有超过 4 MiB 的 8 位 RGB/RGBA 静态 PNG 才尝试 JPEG（质量参数 0.95），保留像素尺寸且体积减少超过 20% 才采用。RGBA / tRNS 按像素检测，存在任何真实透明像素就保留原图；动画、索引色、其他格式或解码失败也保留原字节。只改变临时上传副本，JPEG 转换属于有损压缩，不能将质量参数解释为保真百分比。
- 图片数组最多同时准备 2 项、全局一次解码压缩 1 张；按原下标回填，任何读取失败均中止该组，不过滤失败项造成参考编号错位。取消排队立即返回，仍在运行的浏览器编码结束并释放位图/画布后才释放解码槽。Base64 原图读取受既有 32 MiB 内存预算约束，压缩后仍执行单图 8 MiB、最多 6 张、合计 24 MiB 限制；媒体工作流保留自身预算。
- 自定义 multipart 图片的文件 MIME 和扩展名跟随实际 data URL 类型，覆盖模板遗留的 PNG 标记；预览与实际请求保持一致，路径、字段名和响应协议不变。标准图片网络提交失败或 HTTP 504 显示实际模型、参考图数量、提交体积及等待时间；原生错误只保留类别与系统错误码，不泄露 URL 参数。结果不确定时不自动重提生成。
- 自定义接口文档导入支持带引号的 cURL `-F` / `--form` 图片文件参数及 `--form-string` 文本参数，保存识别出的参考图模式；单个 `image` 映射第一张，`image[]`、复数字段或同名重复文件字段映射全部参考图。示例文件路径不读取、不保留；未知文件字段或无法解析的表单语法明确拒绝并提示手动配置。验证入口：`modelProtocolImport.test.ts`、`modelProtocol.test.ts`、`modelRequestTransport.test.ts`。
- Seedance 2.0/2.5 的分辨率、比例、时长、参考素材与操作级约束集中在 `seedanceModelCapabilities.ts`；火山直连和中转站只在共享语义上叠加各自已确认的字段、路径和响应映射，不能因为模型同名就共用请求体。火山 2.5 支持 `-1` 自动时长，首尾帧及视频编辑按官方约束收敛为自适应比例，视频编辑只允许自动时长；APIMart 保留已确认的 480p/720p、5 秒默认值和自身任务协议，不扩张为火山全集。
- 自定义视频模型可在“视频参数能力”中显式应用“Seedance 版本 + 火山原生/APIMart/Lec”快速模板，一次写入能力与提交/轮询协议。模型目录、手动添加、对话助手和 MCP 共用同一自动匹配器：精确模型 ID 覆盖优先于已验证网关协议，已有手工能力、协议和非视频分类不覆盖；未知网关只补官方家族能力，不猜请求协议。助手与 MCP 通过 `provider_config_preview` 的可选 `templateId` 生成任务级草稿，再由既有 `provider_config_apply`、Policy 和审计链写入。火山模板使用 `content` 类型数组，APIMart 模板使用 `prompt`、`size`、`image_with_roles` 等中转字段，Lec 使用 `/v1/videos` 任务协议并按已核验线路限制参考素材。
- MiniMax H3/H3-Max 的公共能力与 MiniMax 官方、APIMart、AI Ping 三种传输模板集中在 `h3ModelCapabilities.ts`。设置页、手动添加、目录同步、助手与 MCP 共用同一识别逻辑：普通 H3/H3-Max 在已验证网关补齐能力和协议，未知中转只补能力，已有手工字段不覆盖。RunningHub 继续使用 operation 级标准模型适配器，AutoDL/ComfyUI 继续使用工作流模板，不与云 API 请求体混用。Context-IR 返回文本，Regeneration 依赖源任务，APIMart 新目录和默认视频选择器不再将二者展示为普通视频生成模型；旧配置的底层执行兼容保留。AI派、MetaSo 所需的轮询成功后二次二进制下载尚未进入声明式协议，本阶段不提供不完整模板。实施与回滚见 [H3 快速适配计划](./plans/2026-09-22-h3-quick-adaptation.md)。
- 取消需要贯穿提交、轮询和回填。模型已生成但保存失败时，应报告保存状态，不能把再次生成当作保存重试。
- API Key 由凭据服务管理，运行记录不携带密钥。工具审批归[Agent Policy](./对话助手-Agent能力实施方案.md)，不由 Provider 改写。
- 火山方舟虚拟人像库配置位于「编辑火山方舟连接」弹窗中，连接信息下方、启用模型上方，是独立的可选配置，不复用模型 API Key；项目名称默认 `default`。AK、SK 只经 Rust `secret_store` 保存，项目名称与区域等非敏感字段保存在厂商配置中。资产管理入口仍位于资产侧栏的「方舟素材库」页。请求集中在 [volcengineAssetLibrary.ts](../src/services/ai/providers/volcengineAssetLibrary.ts)。由于方舟文档页面的 schema 需要登录后动态加载，当前适配器的签名、路径、字段和 multipart 上传合同在真实接入前必须以官方 OpenAPI/SDK 再校准；不得根据当前 UI 结构推断生产接口字段。
- 火山方舟虚拟人像采用“角色绑定、节点无感使用”方案：角色编辑弹窗可选绑定一个通过真人面部审核的方舟视觉 Asset ID，项目名默认 `default`；视频节点仍只选择普通角色，生成前仅当 Provider 为火山方舟、模型为 Seedance 2.0/2.5、视觉资产为 `Active` 且项目一致时自动改用 `asset://<Asset ID>`，否则回退现有普通参考图流程。角色声音继续使用现有角色声音片段、普通音频或模型音频能力，不绑定方舟 Audio Asset ID。绑定数据只保存视觉 Asset ID、名称、状态和项目名，不保存 AK/SK。用户不直接选择虚拟人像素材，也不需要手写 Asset ID。旧版节点中的直接方舟素材引用保留读取兼容，但新入口不再提供。

## 配置与运行状态

项目设置的图片、视频、音频默认模型支持按类型选择已导入的 ComfyUI 工作流，保存为 `comfyui/<工作流 ID>`；新节点继承时转换为执行模型及 `workflowId`，无模型节点的快捷生成也传递该绑定。设置页允许离线配置，已删除的选择沿用不可选的「已隐藏」提示；文本与视觉理解不提供工作流选项。沿用节点已有模型优先的生成规则，回归入口：`projectSettingsService.test.ts`、`generationProjectDefaults.test.ts`。

本次默认工作流验证：项目设置、节点生成、视频比例、风格参考和分镜生成的 5 个测试文件共 40 项通过，前端/测试类型与定向 ESLint 通过；未执行桌面视觉验收或真实 ComfyUI 调用。

用户配置见[说明书：自定义接口](../site/manual.html#connection)和[本地与云端工作流](../site/manual.html#comfy)。厂商模型、RunningHub 工作流和通用工作流 API 的 Key、参数与恢复状态分别处理，不能互相套用。

模型选择器的工作流分组默认收起，无可用条目时整体隐藏。ComfyUI 工作流按实际绑定地址探测 `/system_stats`，仅展示在线服务对应的条目；未绑定或服务器已删除时沿用默认地址回落。打开菜单立即检测，打开期间每 10 秒刷新，关闭后取消检测；RunningHub 与工作流 API 不受 ComfyUI 在线状态影响，已有节点选择不因暂时离线被清空。验证入口：`modelSelectorWorkflows.test.tsx`、`comfyServerBinding.test.ts`。

ComfyUI 提交前只绕过能够确认的空可选媒体分支；必填或未知结构交由原工作流校验。视频节点未设置秒数或旧帧数时使用 5 秒默认值，旧帧数兼容换算。实时百分比属于当前执行节点，任务完成仍需查询输出并保存成功；具体注入和回退规则见[ComfyUI 集成](./ComfyUI工作流集成说明.md)。

ComfyUI 结果交付要求 history 终态；视频只接受真实视频扩展名，不把预览 PNG 当成成片。默认保留服务缓存；设置页提供显存状态和服务级手动释放，自动卸载或完全释放需显式选择、只对本机回环地址生效，并在队列非空时跳过。释放 history 不等于释放显存，项目不把二者混用。

## 验证与资料

- 工作流 API：通用独立连接，可添加多个平台及命名工作流，自由配置提交/查询路径、鉴权、参数与结果映射。复用 modelProtocol 编辑器和执行器，支持同步/异步 JSON 媒体 URL 输出、图片/视频/音频参数控件；H3 为可编辑模板，旧配置保持兼容。本地素材复用上传服务，整数组和完整编号映射均可发送多图；任务恢复绑定提交时的协议，未知提交不自动重提。前端类型与定向 ESLint 通过，按用户要求未运行本阶段测试及真实服务调用。配置示例、恢复和回滚见[工作流 API 计划](./plans/2026-09-08-autodl-workflow-api/task_plan.md)；源码：[通用定义](../src/services/workflowApi/workflowApiDefinition.ts)、[执行与恢复](../src/services/workflowApi/workflowApiAdapter.ts)，既有测试入口：[配置与分享](../tests/services/workflowApiConfig.test.ts)、[设置入口](../tests/components/providerWorkflowSection.test.tsx)。
- RunningHub 云工作流与 AI 应用：支持定义导入、类型化参数、图片/视频/音频官方上传、节点与对话调用、多产物保存及任务恢复。旧本地工作流保持 ComfyUI 语义；工作流与标准模型使用各自适配器。状态、验收缺口、后续模型扩展与回滚见[RunningHub 平台计划](./plans/2026-09-08-runninghub-platform.md)；测试入口：[定义导入](../tests/services/runninghubWorkflowService.test.ts)、[任务生命周期](../tests/services/runninghubWorkflow.test.ts)。
- RunningHub 标准模型：347 个媒体操作与 7 个旧图片选择共用 manifest、专属参数和媒体 Registry；已有启用选择保留。标准模型用 v2/query 恢复，批量未知提交不自动重提，标准停止等待与工作流取消分开；33 个专项/弃用操作及真实服务验收缺口见上述平台计划。测试入口：[模型合同](../tests/services/runninghubModelManifest.test.ts)、[标准执行与恢复](../tests/services/runninghubMedia.test.ts)。
- APIMart Omni：内置目录以 `gemini-omni-1.1-flash`、`gemini-omni-1.1-flash-ext`、`gemini-omni-flash-preview` 替换旧 `Omni-Flash-Ext`；旧画布 ID 兼容到新 Ext，旧目录选择在设置页迁移，未选择的模型不自动启用。Google 直连目录独立保留。
- 能力及请求合同集中在 [apimartVideoModels.ts](../src/services/ai/apimartVideoModels.ts)：Flash/Preview 不提交时长；Ext 仅允许 4/6/8/10 秒，引用视频时省略时长。Flash 图片含首尾帧合计最多 10 张，Ext 参考图仅 1 或 3 张，Preview 最多 16 张；均最多 1 段参考视频、不接受参考音频。Flash 视频最长 10 秒，Preview 视频 1–24 秒，由现有媒体元数据校验拦截。素材上传、取消与轮询复用 APIMart Adapter。
- Omni 本次接入现有文生、图生、视频参考和 Flash 首尾帧入口；按任务 ID 续写（`extend_from_task_id`）尚无独立产品入口。定向回归 6 个文件、102 项测试通过；未执行真实付费生成或桌面视觉验收。回滚时撤销本批代码；已迁移的模型选择可在设置中重新勾选，不修改画布历史。
- 接口依据：[Flash](https://docs.apimart.ai/cn/api-reference/videos/gemini-omni-1.1-flash/generation)、[Ext](https://docs.apimart.ai/cn/api-reference/videos/omni-flash-ext/generation)、[Preview](https://docs.apimart.ai/cn/api-reference/videos/gemini-omni-flash-preview/generation)；测试入口：[APIMart 生成](../tests/services/apimartGen.test.ts)、[模型目录](../tests/components/defaultModels.test.ts)。
- CCC API 内置 `gpt-image-2.5-flare`、`gpt-image-2.5-sunburst` 与 `gpt-image-2` 无参考图时走 `/images/generations`，有参考图时走 `/images/edits`，按顺序用 multipart `image[]` 上传文件。仅发送标准 `model/prompt/n/size` 参数；像素尺寸使用 `size`，不注入 Grok `resolution` 或用 `2K` 代替 `quality`。统一解析 URL / Base64 图片结果，避免旧 profile 的纯 URL 响应映射丢失图片。测试覆盖旧请求模式/profile、两图/三图顺序、参数字段与自定义连接隔离，入口：[CCC API 模型目录](../tests/services/providerCatalogService.test.ts)、[标准图片请求](../tests/services/modelRequestTransport.test.ts)。
- 接口依据：[OpenAI Images](https://developers.openai.com/api/docs/guides/image-generation) 与 [CCC 通用图片接口](https://cccapi.cn/legal/api-docs#image-generation)。CCC 的 GPT Image 2.5 专章另描述 `image_urls` 异步接口，但未给出完整查询地址和响应合同，本次不猜测或自动切换。相关 101 项离线回归、前端/测试类型与定向 ESLint 检查通过；真实 Key、参考图效果、透明背景和上游 504 仍需服务端验收，离线请求测试不能证明 CCC 当前部署接受请求。需要独立异步协议时应使用自定义连接。

### GRSAI 参考图与跨厂商复用

- GRSAI 已确认的图片型号走 `/v1/api/generate`，以 `images` 数组提交本地优先的 Base64 data URL，`replyType=json`，从 `results[].url` 读取结果。复用 `imageUtils` 的 6 张、单图 8 MiB、合计 24 MiB 本地边界；这是应用限制，不表示服务商上限。未收录型号保持原配置，不自动扩展模型目录。
- Nano Banana 映射 `aspectRatio` 与 `imageSize`；基础 `gpt-image-2` / `gpt-image-2.5` 按服务商能力使用 1K 比例，VIP / Flare / Sunburst 按厂商像素表把档位映射到 `aspectRatio`，不发送 Grok 的 `resolution`。GPT 基础款质量为 `auto`，增强款使用文档支持的 `medium`。
- JSON 请求和响应解析复用 `executeModelProtocol`，不另建传输实现。仅 `succeeded` 可交付，`failed` / `violation` 保留错误，未完成任务提示先查服务商记录。批量按数量逐张执行，失败即停止提交，保留已有成功结果；取消、504 和结果未知均不自动重放。此轮保持同步 JSON，不新增异步任务恢复能力。
- 不能仅按“OpenAI 兼容”统一请求体：GRSAI 兼容 generations 的参考图字段为 `image`，原生接口为 `images`；Grok 原生 edits 是 JSON `images` 对象数组且使用 `aspect_ratio/resolution`；Google Gemini 使用自身内容结构与鉴权。相同协议复用执行器，差异集中在合同。现有 Google/Grok 内置图片协议未接入参考图，本轮仅核对，未改其功能。
- 来源：[GRSAI Nano Banana](https://qmy27nhsd9.apifox.cn/452392911e0)、[GPT Image](https://qmy27nhsd9.apifox.cn/452409160e0)、[兼容 Images](https://qmy27nhsd9.apifox.cn/512802278e0)、[Google 图像](https://ai.google.dev/gemini-api/docs/image-generation)、[Grok 多图编辑](https://docs.x.ai/developers/model-capabilities/images/multi-image-editing)。测试入口：`imageRequestContracts.test.ts`、`modelRequestTransport.test.ts`。真实付费生成、参考图保真与上游超时尚未验收。
- 验证：合同、传输、目录、参考图上传、工作流及 Google/xAI 回归共 17 个正式测试文件、427 项通过；压缩边界与跨入口请求验证见 `referenceImageUpload.test.ts`、`referenceImageTransport.test.ts`，覆盖多图顺序、失败/取消、真实 alpha 判定、预算及 MIME/后缀。编解码使用模拟夹具，不代表实际画质验收。前端与测试类型、定向 ESLint、UTF-8 和差异检查通过。回滚仅撤销本批补丁，无用户配置迁移。

- 定向回归：[生成入口测试](../tests/services/generationRuntime.test.ts)、[模型协议测试](../tests/services/modelProtocol.test.ts)。新增适配还需覆盖输入映射、取消、异常响应与保存失败；真实厂商调用单独记录。
- 专项资料：[媒体 Registry](./plans/2026-07-23-media-provider-registry.md)、[自定义模型协议](./plans/2026-07-19-custom-model-execution-protocol.md)、[视频输入校验](./plans/2026-08-26-video-input-validation-implementation.md)、[ComfyUI 集成](./ComfyUI工作流集成说明.md)。
- 历史：[生成取消](./history/2026-09-07-跨模块实施记录归档.md#media-cancel)、[Provider Registry](./history/2026-09-07-跨模块实施记录归档.md#media-registry)、[Sora2U](./history/2026-09-07-跨模块实施记录归档.md#models-sora2u)、[Canonical Request](./history/2026-09-07-跨模块实施记录归档.md#models-canonical)、[CCCAPI](./history/2026-09-07-跨模块实施记录归档.md#models-cccapi)。

返回[文档导航](./文档导航.md)。
